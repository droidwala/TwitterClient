<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/toolbar"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/colorPrimary"
    app:theme="@style/Theme.AppCompat.Light.DarkActionBar"
    app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
    android:elevation="@dimen/toolbar_elevation"
    android:layout_gravity="start|top">

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <ImageView
            android:id="@+id/logo_img"
            android:layout_width="@dimen/toolbar_title_image_width_height"
            android:layout_height="@dimen/toolbar_title_image_width_height"
            android:layout_gravity="center_vertical"
            android:src="@drawable/ic_title_logo_signup"/>
        <TextView
            android:id="@+id/page_title"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginLeft="@dimen/toolbar_title_margin_left_with_image"
            android:layout_marginStart="@dimen/toolbar_title_margin_left_with_image"
            android:layout_gravity="center_vertical"
            android:gravity="center"
            android:textSize="@dimen/toolbar_title_text_size"
            android:textColor="@color/colorPrimaryDark"
            tools:text="HOME"/>
    </LinearLayout>

</android.support.v7.widget.Toolbar>